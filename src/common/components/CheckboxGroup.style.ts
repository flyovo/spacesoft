import styled from 'styled-components';

export const StyledCheckboxGroup = styled.div`
  display: flex;
  align-items: center;
  gap: 2.4rem;

  & > label {
    width: auto;
    display: inline;
  }
`;
